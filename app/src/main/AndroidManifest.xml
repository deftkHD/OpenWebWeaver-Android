<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.deftk.openlonet">

    <uses-permission
        android:name="android.permission.AUTHENTICATE_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.MANAGE_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission
        android:name="android.permission.USE_CREDENTIALS"
        android:maxSdkVersion="22" />

    <application
        android:name=".OpenLoNet"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.TokenLoginActivity" />
        <activity android:name=".activities.feature.mail.WriteMailActivity">
            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="mailto" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.feature.FilesActivity" />
        <activity android:name=".activities.feature.forum.ForumPostsActivity" />
        <activity android:name=".activities.feature.MembersActivity" />
        <activity android:name=".activities.feature.tasks.EditTaskActivity" />
        <activity android:name=".activities.feature.board.EditNotificationActivity" />
        <activity android:name=".activities.feature.forum.ForumPostActivity" />
        <activity android:name=".activities.feature.mail.ReadMailActivity" />
        <activity android:name=".activities.feature.board.ReadNotificationActivity" />
        <activity android:name=".activities.feature.tasks.ReadTaskActivity" />
        <activity
            android:name=".activities.StartActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="de.deftk.openlonet.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <provider
            android:name=".feature.filestorage.integration.ApiDocumentsProvider"
            android:authorities="de.deftk.openlonet.filestorage.fileprovider"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <service
            android:name=".auth.AuthenticatorService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>
    </application>

</manifest>